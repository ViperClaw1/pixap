/** Bundled placeholder for business place thumbnails (offline-safe). */
export const PLACE_IMAGE_FALLBACK = require("../../../assets/web/placeholder.png");

export const businessCardPlaceholderSource = PLACE_IMAGE_FALLBACK;
