/** FlashList `estimatedItemSize` presets — keep at module scope (not inside components). */
export const FLASH_LIST_ESTIMATED_SIZE = {
  placeRow: 88,
  messageRow: 80,
  messageBubble: 72,
  cartService: 120,
  cartShopping: 72,
  categoryPlace: 112,
  bookingCard: 148,
  shoppingItem: 80,
  feedPost: 360,
  storyComment: 160,
  aiBookingChat: 100,
} as const;
