/** Max height of the story discussion glass sheet (fraction of window height). */
export const STORY_DISCUSSION_SHEET_MAX_HEIGHT_RATIO = 0.8;

/** Min height when content is short (fraction of window height). */
export const STORY_DISCUSSION_SHEET_MIN_HEIGHT_RATIO = 0.5;
