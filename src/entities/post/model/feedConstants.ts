/** Posts loaded per infinite-scroll page (Instagram-style). */
export const FEED_PAGE_SIZE = 12;

/** Max pages kept in TanStack Query cache for the feed. */
export const FEED_MAX_CACHED_PAGES = 40;
